راهنمای قوانین و قراردادهای پروژه (Project Rules & Conventions)
================================================================

این فایل منبع حقیقی (Source of Truth) برای قراردادهای پروژه است.
قبل از هر تغییری در کد، نام‌گذاری یا منو، این فایل را بخوان و دقیقاً از آن اطاعت کن.
اگر کاربر قانونی را تغییر داد، همین فایل را آپدیت کن.


۱. زبان برچسب‌های منوی پنل ادمین
─────────────────────────────────
- برچسب‌های منوی کناری پنل ادمین باید **فارسی** بمانند (نه فینگلیش، نه انگلیسی).
- منبع حقیقی منو: فایل `modir_tanzimat.json` (کلید `menu[]`).
- تابع `menu_get_default_admin_items` فقط وقتی استفاده می‌شود که فایل
  `modir_tanzimat.json` وجود نداشته باشد؛ پس هرگز منو را از روی تابع پیش‌فرض
  ویرایش نکن — فقط فایل JSON را ویرایش کن.
- هر تغییر در منو (اضافه/حذف/تغییر نام) فقط در `modir_tanzimat.json` اعمال شود.
- زیرمنوی «فروشگاه» (والد `s4`) شامل این موارد است:
    • محصول جدید  → mod/store/products/edit   (آیکون fa-plus، والد s4، _id s31)
    • لیست محصولات → mod/store/products        (والد s4، _id s5)
    • دسته‌بندی‌ها / برندها / سفارشات / تنظیمات فروشگاه
- ادیتور منو (drag-and-drop با ماوس):
    • مسیرهای درست (یکپارچه):
        - منوی پنل ادمین  → mod/menu_editor/admin
        - منوی سایت         → mod/menu_editor/site
    • مسیرهای قدیمی (منسوخ — دیگر استفاده نشوند): mod/theme/menu ، mod/menu_editor/admin
    • فایل ادیتور: mohtava/menu/menu-editor.php
        - تابع منوی پنل:  menu_editor_admin()
        - تابع منوی سایت: menu_editor_site()
    • کتابخانهٔ جابجایی: ghaleb/manabe/Sortable.min.js (لوکال — نه CDN)
        - نکته: قبلاً از CDN (cdn.jsdelivr.net) لود می‌شد که روی لاراگون/آفلاین کار
          نمی‌کرد؛ حتماً نسخهٔ لوکال را نگه دارید و لینک CDN را برنگردانید.
    • اگر آیتم جدیدی به منو اضافه می‌کنید، هم در default
      (menu_get_default_admin_items در menu-editor.php) و هم در modir_tanzimat.json
      لینک را با مسیر درست (mod/menu_editor/...) بنویسید.


۲. قرارداد نام‌گذاری فایل‌ها (فینگلیش)
──────────────────────────────────────
- نام فایل‌های پروژه باید **فینگلیش** باشد: کلمات فارسی نوشته‌شده به حروف لاتین
  (مثل tanzimat، nasb، modir) — نه انگلیسیِ معمول (settings، install) و نه فارسیِ
  اسکریپت.
- نمونه‌های جایگزینی‌شده (نام فعلی معتبر):
    • settings.php        → tanzimat.php                 (تنظیمات — همه‌چیز: هسته + تنظیمات سایت + توابع + پنل)
    • install.php         → nasb.php                     (نصب)
    • admin_settings.json → modir_tanzimat.json         (تنظیمات مدیر — منوی پنل + نمایش پنل)
    • config.php         → pikan_tanzimat.php           (پیکان‌تنظیمات — فقط alias که tanzimat.php را لود می‌کند)
    • router.php         → masiryab.php                 (مسیریاب — حذف شد، مستقیم به masiryab.php اشاره شود)
    • helpers.php        → tavabe.php                   (توابع — حذف شد، مستقیم به tavabe.php اشاره شود)
    • site_settings.php   → ادغام در tanzimat.php       (حذف شد؛ تنظیمات سایت و توابع کمکی در tanzimat.php هستند)
    • cp.php             → panel.php                    (پنل — اسکریپت مدیریت ادمین)
    • autoload.php      → bargozari.php                (بارگذاری خودکار کلاس‌ها)
    • admin/           → modir/  (پوشه پنل؛ controllers/admin.php → modir/controllers/modir.php)
    • database/         → payegah_dade/  (پوشه مایگریشن‌ها؛ refs در nasb.php)
    • backups/          → poshtyban/  (پوشه پشتیبان‌گیری)
    • payegah/          → حذف شد (کپی اضافی migrationها؛ اصلی در payegah_dade/migrations/ است)
    • content/          → حذف شد (پوشه خالی)
    • elpayaagh_tanzimat.php (تنظیمات اطلاع‌رسانی — در elpayaagh/)
    • sarsafhe.php (ghmod + mehrsam) → sarfaraz.php  (سرصفحه/هدر قالب)
    • mohtava/seo-settings.php → mohtava/seo_tanzimat.php  (تنظیمات SEO؛ در bargozari.php classMap)
    • کانال‌های اطلاع‌رسانی (afzuneh/notification/channels/):
        BaleChannel→kanal_bale ، PushChannel→kanal_push ، SmsChannel→kanal_payamak ،
        TelegramChannel→kanal_telegram ، WhatsAppChannel→kanal_whatsapp
    • درگاه‌های پرداخت (afzuneh/pardakht-darbe/):
        PaymentGateway→darbe_pardakht ، ZarinPalGateway→darbe_zarinpal ،
        IdPayGateway→darbe_idpay ، ZibalGateway→darbe_zibal
- فایل‌هایی که عمداً انگلیسی نگه داشته شدند (نام برند/معمول):
    • zarinpal.php (زرین‌پال فینگلیشه = zarin+pal) + helpers/payment/zarinpal.php (alias)
    • index-en.php (نسخه انگلیسی سایت — طبق قرارداد قبلی)
- نکته: فایل‌های انگلیسی `config.php` / `router.php` / `helpers.php` فقط alias (پیوند) بودند
  و حذف شدند. نام فایل اصلیِ فینگلیش را بکار ببر:
    - tanzimat.php  (بجای config.php)
    - masiryab.php (بجای router.php)
    - tavabe.php    (بجای helpers.php)
- اگر فایل جدیدی می‌سازی یا فایلی را تغییر نام می‌دهی، از همین فرمت فینگلیش استفاده کن.


۳. چک‌لیست تغییر نام فایل (OBLIGATORY)
──────────────────────────────────────
وقتی فایلی را تغییر نام می‌دهی، حتماً موارد زیر را انجام بده وگرنه سایت می‌شکند:
1. با grep کل پروژه را جستجو کن و تمام ارجاع‌ها را پیدا کن:
     گردونه‌های require / require_once / include / include_once
     تعریف‌های define(...) که مسیر فایل را می‌سازند
     رشته‌های مسیر در اسکریپت نصب (nasb.php) و هر جای دیگر
2. همهٔ ارجاع‌ها را به نام جدید آپدیت کن.
3. اسکریپت نصب (`nasb.php`) را فراموش نکن: ممکن است فایل را با
   file_put_contents بنویسد یا به آن ارجاع دهد.
4. بعد از تغییر، حتماً تست کن:
     php -l روی فایل‌های تغییر‌یافته
     اجرای یک اسکریپت کوچک که فایل را require می‌کند و ثابت‌ها/منو بارگذاری می‌شود.
5. چک کن هیچ ارجاعی به نام قدیمی باقی نمانده باشد.


۴. نکات فنی مهم (از اشتباهات تکراری جلوگیری کن)
──────────────────────────────────────────────
- منوی پنل: تابع پیش‌فرض را ویرایش نکن؛ فایل `modir_tanzimat.json` را ویرایش کن.
- صفحه‌ساز (builder): محتوای بوم از ستون `blocks_data` خوانده می‌شود
  (در فایل `builder.php`: `var blocksData = json_encode($blocks)`).
  اگر `blocks_data` خالی باشد ولی `cached_html` پر، بوم خالی می‌ماند در حالی که
  پیش‌نمایش محتوا را نشان می‌دهد.
- ذخیره‌سازی هوش مصنوعی‌های مختلف: چون ممکن است چند AI روی یک فایل تغییر بدهند،
  همیشه ابتدا فایل فعلی را بخوان و با منبع حقیقی (این راهنما + فایل‌های ذکر شده)
  هماهنگ کن. از بازنویسی کورکورانهٔ تغییرات دیگران پرهیز کن.
- به‌روزرسانی (git / تب «Be-Rooz Resani»): مراقب باش که نسخهٔ ریموت فایل‌ها را
  با نام‌های قدیمی بازنویسی نکند. بعد از اعمال تغییرات، تغییرات را کامیت کن.


۵. اولویت منبع حقیقی
─────────────────────
هنگام تضاد اطلاعات، به این ترتیب عمل کن:
1. این فایل راهنما (rahnamay.txt)
2. فایل‌های پیکربندی پروژه (modir_tanzimat.json و tanzimat.php)
3. کد موجود در پروژه
4. صحبت‌های گذشته (ممکن است منسوخ شده باشند)


۶. قوانین ویرایشگر واحد
─────────────────────
- **فقط یک ویرایشگر:** صفحه‌ساز (Page Builder) ویرایشگر رسمی تمام محتواست.
- ویرایشگرهای HTML قدیمی حذف شده‌اند و نباید برگردند.
- منوی جداگانه "صفحه‌ساز" در سایدبار وجود ندارد؛ صفحه‌ساز داخل فرم ویرایش هر نوع محتوا ادغام شده است.
- **محصول/وبلاگ:** فیلدهای ثابت (عنوان، قیمت، دسته و...) بالا + صفحه‌ساز برای محتوا پایین.
- **صفحه/خدمت:** صفحه‌ساز کامل (کل صفحه با بلاک).
- **خدمات:** جدول `khadamat` منسوخ است. خدمات با `posts.type='khadamat'` ذخیره می‌شوند و از طریق `mod/services` مدیریت می‌شوند.


۷. قوانین ریسپانسیو صفحه‌ساز
─────────────────────
- صفحه‌ساز دارای ۶ حالت دستگاه است:
    • عریض (Wide) — 1920px
    • دسکتاپ (Desktop) — 1366px
    • تبلت افقی (Tablet Landscape) — 1024px
    • تبلت عمودی (Tablet Portrait) — 768px
    • موبایل افقی (Mobile Landscape) — 844px
    • موبایل عمودی (Mobile Portrait) — 390px
- این ۶ حالت نباید حذف یا کاهش یابند.


۸. قوانین ساختار محتوا
─────────────────────
- تمام انواع محتوا (مطلب، صفحه، محصول، خدمت) باید از فرآیند ویرایش یکسان پیروی کنند.
- فرم ایجاد/ویرایش شامل دو بخش است:
    1. فیلدهای پایه (بسته به نوع محتوا)
    2. صفحه‌ساز برای محتوای بصری
- دکمه "ذخیره و رفتن به صفحه‌ساز" در فرم‌های ایجاد محتوا الزامی است.


۹. اطلاعات فنی پروژه
─────────────────────
- **نام پروژه:** مهراد سام (Mehrad Sam) — نام داخلی LaRaGoRn
- **پایگاه داده:** mehrsamdb
- **قالب پیش‌فرض:** mehrsam
- **نسخه:** ۱.۰
- **فناوری:** PHP 7.4+ خالص (بدون فریم‌ورک) + MySQLi + Apache .htaccess
- **کدگذاری:** UTF-8 بدون BOM

### ۹.۱ ساختار دایرکتوری
```
مهراد سام/
├── index.php                  # ورودی اصلی (Front Controller)
├── nasb.php                   # نصاب (Installer)
├── panel.php                  # مدیریت ادمین از خط فرمان (CLI)
├── rahnamay.txt               # قوانین و قراردادهای پروژه (این فایل)
├── sw.js                      # سرویس‌ورکر Web Push (ثبت اشتراک + نمایش اعلان)
├── manifest.json              # فایل PWA manifest (اطلاعات اپلیکیشن وب)
│
├── haste/                     # هسته (Core Engine)
│   ├── tanzimat.php           # تنظیمات پایه (DB، مسیرها، توابع)
│   ├── masiryab.php           # مسیریاب (Router)
│   ├── tavabe.php             # توابع عمومی (redirect، احراز هویت)
│   ├── site_settings.json     # تنظیمات سایت
│   ├── modir_tanzimat.json    # تنظیمات پنل ادمین (منو، رنگ، فونت)
│   └── mod/                   # ماژول ادمین
│       └── mod.php            # کنترلر اصلی ادمین
│
├── ghaleb/                    # قالب‌ها (Themes)
│   ├── ghmod/                 # پوسته پنل ادمین
│   ├── mehrsam/               # قالب پیش‌فرض
│   └── manabe/                # منابع مشترک (فونت، فاوآیکن، Sortable.js)
│
├── mohtava/                   # محتوا (Content Modules)
│   ├── mohtava-kontrol.php    # کنترلر عمومی محتوا
│   ├── tarnegar/              # ماژول وبلاگ
│   ├── khadamat/              # ماژول خدمات فرانت‌اند
│   ├── tamas/                 # ماژول تماس
│   ├── forushgah/             # ماژول فروشگاه
│   ├── gheychat/              # ماژول چت
│   ├── file/                  # ماژول مدیریت فایل
│   ├── menu/                  # ماژول منو
│   ├── ghaleb/                # ماژول مدیریت قالب
│   ├── sakhtar/               # ماژول صفحه‌ساز
│   ├── poshtyban/             # ماژول پشتیبان
│   │   ├── backup-admin.php   # بکاپ و بازگردانی (ZIP کامل: دیتابیس + فایل‌ها)
│   │   ├── backup-functions.php # توابع بکاپ
│   │   └── update-admin.php   # آپدیت و نگهداری (ZIP ادغامی + SQL مهاجرت + بکاپ SQL)
│   └── seo_tanzimat.php       # تنظیمات SEO
│
├── dade/                      # دیتابیس (Database)
│   └── bank.php               # کلاس اتصال MySQLi (کلاس Bank)
│
├── afzuneh/                   # افزونه‌ها (Plugins)
│   ├── elpayaagh/             # اعلان‌رسان (تلگرام، واتساپ، بله، پوش، پیامک)
│   │   ├── Notifier.php       # کلاس اصلی اعلان (اعلان چت/تماس/سفارش → همه کانالها)
│   │   ├── webpush.php        # موتور رمزنگاری Web Push (VAPID + aes128gcm)
│   │   ├── push-admin.php     # صفحه mod/push (فعالسازی/تست)
│   │   ├── openssl.cnf        # کانفیگ مینیمال openssl برای ویندوز
│   │   └── channels/          # کانالهای ارسال
│   │       ├── kanal_push.php
│   │       ├── kanal_bale.php
│   │       ├── kanal_telegram.php
│   │       ├── kanal_payamak.php
│   │       └── kanal_whatsapp.php
│   └── pardakht/              # درگاه‌های پرداخت (زرین‌پال، آیدی‌پی، زیبال، نکست‌پی)
│
└── payegah_dade/              # مایگریشن‌های دیتابیس
```

### ۹.۲ مسیریابی
```
URL ورودی        → ماژول مربوطه
─────────────────────────────────────
mod/...          → haste/mod/mod.php (پنل ادمین)
karbar/...       → haste/karbar/karbar.php (پنل کاربری)
khadamat/...     → mohtava/khadamat/khadamat-kontrol.php (صفحات خدمات فرانت‌اند)
mohtava/...      → mohtava/mohtava-kontrol.php (صفحات محتوا)
forushgah/...    → mohtava/forushgah/ (فروشگاه)
chat/...         → mohtava/gheychat/chat-kontrol.php (چت)
tarnegar/...     → mohtava/tarnegar/tarnegar-kontrol.php (وبلاگ)
tamas/...        → mohtava/tamas/tamas-kontrol.php (تماس)
home / ''        → صفحه اصلی (safhe_khane())
سایر مسیرها      → 404.php
```

### ۹.۳ مسیرهای پنل ادمین
```
mod/dashmod     → داشبورد اصلی
mod/lomod       → صفحه ورود
mod/content     → مدیریت مقالات
mod/edit_content/{id} → ایجاد/ویرایش محتوا (شامل صفحه‌ساز)
mod/pages       → مدیریت صفحات
mod/edit_page/{id} → ایجاد/ویرایش صفحه (شامل صفحه‌ساز)
mod/services    → مدیریت خدمات
mod/edit_service/{id} → ایجاد/ویرایش خدمت (شامل صفحه‌ساز)
mod/store/...   → مدیریت فروشگاه
mod/theme/...   → مدیریت قالب
mod/files       → مدیریت فایل
mod/backup      → پشتیبان‌گیری
mod/update      → آپدیت و نگهداری (ZIP ادغامی، SQL مهاجرت، بکاپ SQL، وضعیت گیت)
```

### ۹.۴ ساختار جداول دیتابیس
```
users           → کاربران (ادمین و کاربر عادی) با رمز bcrypt
posts           → محتوای یکپارچه (وبلاگ، مجله، صفحه، مقاله، خدمات)
categories      → دسته‌بندی وبلاگ
post_categories → رابطه مقاله و دسته
mahsulat        → محصولات با قیمت، تخفیف، موجودی
mahsul_dasteh   → دسته‌بندی محصولات
mahsul_brand    → برندهای محصولات
sabad           → سبد خرید
sabad_mahsul    → آیتم‌های سبد خرید
sefaresh        → سفارشات
sefaresh_mahsul → آیتم‌های سفارش
payam_tamas     → پیام‌های فرم تماس
chat_sessions   → جلسات چت
chat_messages   → پیام‌های چت
block_pages     → داده‌های صفحه‌ساز (JSON blocks)
template_sections → بخش‌های محتوایی قالب
push_subscriptions → اشتراک‌های Web Push (endpoint, p256dh, auth, user_agent, label)
```

### ۹.۵ صفحه‌ساز (Page Builder) — نگارش ۲ (سبک المنتور)
- **محل فایل:** mohtava/sakhtar/builder.php + blocks/block-types.php + inline-editor.js (موتور ویرایش زنده داخل پیشنمایش)
- **معماری:** پنل چپ ~۲۰٪ قابل تغییر اندازه (پالت بلاک + تنظیمات بلاک انتخابی + تب ⚙️ تنظیمات) + پیشنمایش زنده iframe = محل اصلی ویرایش
- **ویرایش مستقیم روی پیشنمایش:** کلیک = انتخاب + تولبار شناور (تنظیمات/تصویر/تکثیر/بالا/پایین/آزاد/حذف با متن)، دابلکلیک متن = ویرایش درجا
- **درگ:** خطی (بین بلاکها، خط راهنمای سبز) با Pointer Capture از پالت؛ Alt+رها = درج آزاد زیر موس؛ دکمه «آزاد» در تولبار = موقعیت X/Y/W/Z برای هر بلاک به تفکیک دستگاه
- **مهم:** هدر/فوترِ قالب هم data-block-index دارند؛ همه محاسبات ایندکس فقط در `.builder-edit-root` (محتوای اصلی) scope میشود — هرگز global نشمار
- **ذخیره‌سازی:** همان JSON `blocks_data` جدول `block_pages` (سازگار با صفحات قدیمی؛ پرچم `free` per-block جدید است)
- **ذخیره دوگانه:** manual کامل (JSON+رندر+sync+کش) / autosave سبک (`light=1` فقط JSON) — فاصله autosave در site_settings.json کلید `builder` (پیشفرض ۱۰ دقیقه، از تب ⚙️)
- **سرعت:** `session_write_close()` ابتدای save/render/preview + یک کانکشن Bank مشترک برای کل رندر (پارامتر `$bank` تا block_*_render پاس میشود)
- **کش رندر:** پیشفرض روشن با TTL ۵ دقیقه (`cached_html` + `cache_updated`)؛ light-save کش را NULL میکند و اولین بازدید بعدی دوباره پر میکند
- **انتخاب تصویر:** مودال سهکاره (کتابخانه `mod/builder/list_images` / آپلود `mod/builder/upload_image` → FILES_DIR/builder/ / URL دستی)
- **۶ حالت ریسپانسیو:** حفظ شده (قانون بخش ۷) — سوییچ دستگاه بالای صحنه؛ عرض iframe با scale-to-fit
- **safhesaz.js مرده حذف شد** — ویرایشگرهای فعال: SadastEditor (فیلدهای html پنل) + inline-editor.js (iframe). فایل editor.php قدیمی فقط در فرمهای کلاسیک mod است.
- **۱۳ نوع بلاک:** heading, text, image, gallery, button, divider, columns, video, services, products, custom, map, counter
- **قالب پیش‌فرض:** mehrsam (قالب شرکتی مدرن RTL)

### ۹.۵.۱ قوانین تغییر صفحه‌ساز
- ساختار JSON بلاکها را نشکن؛ سازگاری عقب لازم است.
- هیچ CDN اضافه نشود (قانون ۹.۸)؛ همه اسکریپتها لوکال.
- تولبار/دکمهها باید متن + آیکون داشته باشند (خوانایی).
- قبل از هر تغییر ساختاری، بخش «مهم» بالا را ببین: scope ایندکسها.

### ۹.۵.۲ سیستم اعلان Web Push (اعلان گوشی مالک)
- **موتور رمزنگاری:** `afzuneh/elpayaagh/webpush.php` — رمزنگاری خالص PHP
  (VAPID ES256 + aes128gcm) بدون وابستگی به Composer/PushLib. روی ویندوز اگر
  `openssl_pkey_new` خطا داد، fallback به `afzuneh/elpayaagh/openssl.cnf`.
- **کلید VAPID:** خودکار در `haste/site_settings.json` > `push.vapid_private` ذخیره
  میشود (تولید اولین بار هنگام فعالسازی از mod/push).
- **صفحه مدیریت:** `mod/push` → `afzuneh/elpayaagh/push-admin.php`
  (فعالسازی/غیرفعالسازی + لیست اشتراکها + تست ارسال به یک دستگاه)
- **سرویس‌ورکر:** `sw.js` در ریشه سایت — ثبت push listener + نمایش اعلان با کلیک
  باز کردن لینک. `manifest.json` (PWA) هم در ریشه.
- **جدول:** `push_subscriptions` (مایگریشن `029-push-subscriptions.sql`)
  — UNIQUE روی endpoint: اشتراک تکراری خودکار رد میشود.
- **ارسال اعلان:** تابع `Notifier::sendPushAll($title,$body,$url)` در `Notifier.php`
  → کانال `kanal_push.php` → حلقه روی همه ردیفهای جدول.
- **نکته:** Web Push فقط با HTTPS کار میکند. روی لوکال (HTTP) ثبت ناموفق است.
- **پروتکل عیب‌یابی:**
  1. mod/push → فعالسازی → اگر کلید VAPID ساخته نشد → openssl اکستنشن فعال است؟
  2. گوشی باز کردن mhsi.ir/mod/push → اگر «اجازه» نداد → مرورگر HTTPS را قبول ندارد
  3. اگر ثبت شد ولی اعلان نرسید → کنسول مرورگر (F12) → Application → Service Workers

### ۹.۵.۳ دکمه پشتیبانی شناور (چندکاناله)
- **فایل:** `ghaleb/mehrsam/poshtibani.php`
- **نحوه نمایش:** `include` در `ghaleb/mehrsam/panevis.php` (فوتر) — فقط در صفحات
  غیرادمین و وقتی ویجت چت فعال باشد.
- **رفتار:** ابتدا حباب چت قدیمی (`headst-chat-bubble`) را با JS مخفی میکند
  (`display:none!important`) و دکمه نارنجی شناور (right:24px) را نشان میدهد.
  کلیک = باز شدن پنل کانالها (چت/تلگرام/ایتا/روبیکا/واتساپ/ایمیل/پیامک/تماس).
- **تنظیمات:** از `site_settings.json` > `support`:
  `enabled`, `channels.{chat,telegram,eitaa,rubika,whatsapp,email,sms,tel}`,
  `colors.btn/bg/hover`, `badge` (پیام بالای دکمه).
- **غیرفعال‌سازی:** اگر `support.enabled=false` باشد، دکمه شناور حذف و حباب چت قدیمی
  برمیگردد (خودکار).
- **مسیرها در کد:**
  - فرم تنظیمات در پنل: `haste/mod/mod.php` → `case 'settings'` → `isset($_GET['tab']) && $_GET['tab']==='messages'`
  - ذخیره POST: whitelist `['support','notif']` → ذخیره در `site_settings.json`.

### ۹.۵.۴ تب «پیام‌ها» در تنظیمات پنل (`mod/settings?tab=messages`)
- **بخشهای تنظیمات:**
  1. کانالهای پشتیبانی (فعال/غیرفعال + شماره/آیدی هر کانال)
  2. رنگ دکمه شناور (رنگ دکمه / رنگ پس‌زمینه / رنگ hover)
  3. تنظیمات ویجت چت (رنگ هدر/پیامها/متن هدر/فونت سایز/متن خوشآمد)
  4. توکن اعلان (بله / تلگرام) — اولویت با site_settings.notif.* (اگر خالی باشد
     از site_settings.bale_token / telegram_token خوانده میشود)
- **暗暗有色:** `Notifier` کلاس اصلی اعلان است → `Notif::notifyAdmin($type,$data,$extra)`
  همه اعلانها (چت، تماس، سفارش) را به کانالهای فعال میفرستد.

### ۹.۶ قالب (Theme)
- **محل فایل:** ghaleb/{name}/
- **ساختار:** info.json, sarfaraz.php (هدر), panevis.php (فوتر), khane.php (خانه)
- **فعال‌سازی:** از `site_settings.json` > `theme.active`
- **شخصی‌سازی:** رنگ‌ها، فونت، لوگو، فاوآیکن، CSS/JS سفارشی

### ۹.۷ فناوری‌های استفاده‌شده
- PHP 7.4+ خالص (بدون فریم‌ورک)
- MySQL / MariaDB از طریق MySQLi
- Apache .htaccess (URL Rewriting)
- HTML5 + CSS3 (RTL)
- JavaScript خالص (بدون jQuery)
- Font Awesome 6.5 (محلی)
- Vazirmatn WOFF2 (فونت فارسی محلی)
- Sortable.js (لوکال، نه CDN)
- bcrypt (password_hash)
- JSON (site_settings.json, modir_tanzimat.json)
- Git (کنترل نسخه + به‌روزرسانی از پنل)

### ۹.۸ قانون منبع داخلی (NO EXTERNAL RESOURCES)
- **هیچ ارتباطی با اینترنت خارجی نباید در زمان اجرا وجود داشته باشد.**
- تمام CSS، JavaScript، فونت، آیکون و تصاویر باید از داخل سرور سایت بارگذاری شوند.
- منابع محلی موجود:
    - `manabe/js/sadastEditor.js` — ویرایشگر متنی ساده و وابسته‌نشده
    - `ghaleb/manabe/Sortable.min.js` — کتابخانه Drag & Drop
    - `ghaleb/manabe/fontawesome/` — فونت آیکون‌ها
    - `ghaleb/manabe/fonts/` — فونت فارسی Vazirmatn
- **منع صریح:**
    - استفاده از `cdn.jsdelivr.net`، `cdn.tiny.cloud`، `cdn.ckeditor.com` یا هر سرویس CDN خارجی
    - بارگذاری کتابخانه‌های جانبی از طریق `<script src="https://...">`
    - بارگذاری فونت یا آیکون از طریق `@import` یا `url(../...)` در CSS
- اگر ابزار جدیدی نیاز است، ابتدا به صورت محلی داخل `manabe/js/` یا `ghaleb/manabe/` اضافه شود.

### ۹.۸ نصب و راه‌اندازی
- **نصب:** اجرای nasb.php → ۴ گام (خوش‌آمدگویی، اطلاعات DB، اطلاعات سایت، تأیید)
- **انتقال:** بکاپ از mod/backup → آپلود → بازگردانی
- **پیش‌نیازها:** PHP 7.4+, MySQL 5.7+, Apache mod_rewrite, حافظه 64MB+
- **امنیت:** بعد از نصب، nasb.php را حذف کنید


۱۰. آپدیت خودکار از گیت و آپدیت و نگهداری سایت
──────────────────────────────────────────────
### ۱۰.۱ کاربر دسترسی خودکار (Agent) روی هاست
- کاربر پنل ادمین برای دسترسی مستقیم Agent:
    - نام کاربری: `mehrsam-bot`
    - رمز: `Bh4#2k!mehrsam-88`
    - ایمیل: `ali.asgari.6106@gmail.com`
    - نقش: admin
    - در دیتابیس لوکال id=3 و روی هاست id=4 است.
- مایگریشن ساخت کاربر: `payegah_dade/migrations/028-bot-user.sql`
- مایگریشن‌ها به ترتیب شماره (0XX) اجرا می‌شوند و یک‌بار اجرا می‌شوند.

### ۱۰.۲ هشدار ایمیل ورود به پنل
- تابع `send_login_alert()` در `haste/tavabe.php` تعریف شده است.
- در **هر ورود موفق** به پنل ادمین (در `haste/mod/mod.php` بعد از session set) صدا زده می‌شود.
- جزئیات ارسالی: نام کاربری، id، IP، User-Agent، زمان (Y/m/d H:i) و تاریخ شمسی.
- ارسال با `mail()` سرور انجام می‌شود؛ خروجی آن در صفحه‌ای جدا به کاربر نمایش داده نمی‌شود.

### ۱۰.۳ بخش «آپدیت و نگهداری» (mod/update)
- فایل: `mohtava/poshtyban/update-admin.php` — تابع `admin_update_route()`.
- روت: `haste/mod/mod.php` → `case 'update'`.
- سه قابلیت:
    1. **آپدیت ZIP ادغامی** (`update_apply_zip`): فایل‌ها را جایگزین/اضافه می‌کند و هرگز حذف نمی‌کند.
    2. **اجرای SQL مهاجرت** (`update_run_sql`): دستورات DROP/TRUNCATE/DELETE/REPLACE/RENAME را به‌طور پیش‌فرض رد می‌کند (با تیک allow_dangerous فعال می‌شود). UPDATE بلاک نمی‌شود.
    3. **دانلود بکاپ SQL** (`sql_download`): یک فایل dump از کل دیتابیس برمی‌گرداند.
- فایل‌های محافظت‌شده از بازنویسی در ZIP (در `update_is_protected`):
    `haste/tanzimat.php`، `haste/site_settings.json`، `haste/modir_tanzimat.json`،
    `haste/dade/bank.php`، `backups/`، `poshtyban/`، `ghaleb/manabe/uploads/`، `nasb.php`
- منوی «آپدیت و نگهداری»: در `haste/modir_tanzimat.json` (والد `s26`، `_id s34`) و منوی پیش‌فرض در `mohtava/menu/menu-editor.php`.

### ۱۰.۴ وضعیت گیت در هاست
- پنل `mod/settings?tab=git` از `exec()` + git استفاده می‌کند؛ در هاست اشتراکی معمولاً
  غیرفعال/نصب نیست و «خطا در ارتباط با سرور» می‌دهد.
- در این صورت **روش ZIP ادغامی** (mod/update) جایگزین است.
- متغیرهای محیطی پنل گیت: `haste/mod/mod.php` → `mod_git_environment()`.

### ۱۰.۵ آپدیت دستی روی هاست (بدون git)
- از سیستم لوکال فایل `backups/update-manual.zip` را بگیر (شامل فقط فایل‌های تغییر یافته).
- در File Manager هاست به ریشه سایت برو، ZIP را آپلود و Extract کن (جایگزینی ادغامی).
- محافظت‌شده‌ها (`haste/tanzimat.php` و... ) داخل این ZIP نیستند؛ دستی جایگزینشان نکن.
- بعد از هر آپدیت، تغییرات را در لوکال کامیت و پوش کن.

### ۱۰.۶ مخزن گیت
- origin: `https://github.com/aliasgari1361/mehrsam.git` — شاخه `main`.
- `haste/tanzimat.php` عمداً **کامیت/پوش نمی‌شود** (کانفیگ لوکال DB داخلش است).
  روی لوکال: `DB_PASS=root`, `DB_NAME=mehrsam_db`؛ در ریپو/هاست: `DB_PASS=''`, `DB_NAME=mehrsamdb`.
===============================================================
آخرین به‌روزرسانی: بازطراحی صفحه‌ساز v2 + سیستم اعلان Web Push (VAPID aes128gcm) + دکمه پشتیبانی شناور چندکاناله + تب پیامها در تنظیمات + فروپاشی موبایل محتوای قدیمی + کش رندر TTL 5دقیقه MySQL-side + حذف BOM + حذف safhesaz.js مرده
